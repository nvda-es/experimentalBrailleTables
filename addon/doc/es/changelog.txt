Esta versión actualiza la tabla de español grado 1, permitiendo escribir puntos en vez de apóstrofos al pulsar el punto 3 en un teclado:

* En es-chardefs.cti, se añade el operador noback al apóstrofo. De esa forma, al pulsar el punto 3 en un teclado se escribirá un punto, mientras que el apóstrofo se sigue representando del mismo modo en una pantalla braille.

Suma de comprobación SHA256: 